-content_for :sidebar do
  =render :partial => "shared/sidebar"
  
.body
  %h2= @event.name
  = flash[:notice] unless flash[:notice].nil?
  
  <p>
  = @event.description
  </p>
  
  <p>
  = link_to 'Sign-Up', new_event_attendees_path(@event)
  </p>

